{
  "name": "oni-dsl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "http-server": "http-server -c-1",
    "parser": "pegjs --format globals --export-var parser syntax.pegjs",
    "parser-trace": "pegjs --format globals --export-var parser --trace syntax.pegjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "codemirror": "^5.42.0",
    "http-server": "^0.11.1",
    "lz-string": "^1.4.4",
    "pegjs": "^0.10.0"
  }
}
